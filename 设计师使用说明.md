# 上海迪士尼 3D 导览 · 设计师使用说明

面向设计 / 产品同事：本地如何打开、如何设 LBS、**哪些数是假数据 / 哪些是真实接口**，当前已实现能力，以及 **Step 1 / 2 / 3 分期与全部后续功能清单**。

> 文档与代码不一致时，以代码为准；可请开发同步更新本文档。

---

## 一、本地怎么跑起来

### 需要安装

| 工具 | 说明 |
|------|------|
| **Node.js** | 建议 18+ 或 20+ |
| **现代浏览器** | Chrome / Safari（手机可用同一 WiFi 访问电脑 IP） |
| **Git** | 拉取代码 |

### 启动

```bash
cd backend
npm install
npm start
```

浏览器打开 **http://localhost:3000**（不要用 Live Server 5500）。

### 环境变量（`backend/.env`）

| 变量 | 用途 | 不配会怎样 |
|------|------|------------|
| `AMAP_WEB_KEY` | 高德步行路线、POI 搜索、模型与地图对齐 | 步行连线不可用；启动时不自动同步 POI |
| `BAIDU_MAP_AK` | 百度步行路线 | 百度步行不可用（可走高德或直线兜底） |
| `FAI_LLM_API_KEY` | 智能行程文案（LLM） | 智能规划 / 路线「一键规划」用默认简短建议 |

---

## 二、数据来源一览（Mock vs 真实 API）

### 总表

| 数据 | 当前来源 | 是否真实 API | 改假数据 / 配置看这里 |
|------|----------|--------------|------------------------|
| **各项目排队时长** | 程序随机 + 场景切换 | ❌ Mock | 见下方「排队」 |
| **项目开/关园状态** | Mock 场景 | ❌ Mock | `backend/config/planner_mock_scenarios.json` |
| **入园时间** | 点「智能规划」时本机当前时刻 | ⚠️ 本机时钟 | `frontend/src/ui/parkTimes.js` |
| **离园 / 闭园时间** | 默认 **21:00** 占位 | ❌ Mock | 同上；后端 `costModel.js` → `DEFAULT_PARK_CLOSE_MINUTES` |
| **迪士尼官方开园/闭园** | 未接入 | ❌ 待做 | 未来 `GET /api/park/hours` |
| **景点名称、分区、描述** | 本地种子 | ❌ 静态 | `backend/db/seed.js` |
| **景点经纬度 GCJ** | 种子 + 可选高德同步 | ⚠️ | `seed.js`；`npm run sync-amap` |
| **游玩时长、身高、刺激、权益标签** | 本地 JSON | ❌ Mock 运营数据 | `backend/data/attraction_metadata.json` |
| **行内步行（推荐算法里）** | 场景直线距离换算 | ❌ 估算 | `costModel.js` → `SCENE_UNITS_PER_WALK_MINUTE` |
| **步行路线（地图上的线）** | 高德 / 百度 | ✅ 需 Key | `backend/.env` |
| **LBS 我的位置** | 默认城堡 + ⇧⌘ 点地 | ❌ 模拟为主 | `geoLocation.js`、`lbsMapPick.js` |
| **手机 GPS** | 浏览器定位 API | ✅ 硬件（未默认主路径） | `geoLocation.js` |
| **机会项** | Mock 排队历史 + 规则 | ❌ | `planner_step2.json`、`opportunityDetector.js` |
| **权益修正后的有效排队** | **未实现** | ❌ Step 3 | 见第十节 |
| **3D 模型** | 本地 `.glb` | — 资产 | `frontend/assets/models/optimized/` |

### 排队时长（Mock）

| 机制 | 文件 |
|------|------|
| 每 5 分钟自动刷新 | `scraperService.js` → `mockFetchWaitTimes()` |
| 演示场景按钮 | `planner_mock_scenarios.json` + 行中面板「演示：排队 / 关闭场景」 |
| 定时任务 | `backend/config/index.js` → `WAITTIME_UPDATE_INTERVAL` |

---

## 三、LBS「我的位置」

### 默认

蓝色图钉在 **奇幻童话城堡**（`geoLocation.js` → `CASTLE_GEO`）。完成 **智能规划** 后才会定标并参与推荐。

### 快捷键

在 **3D 地面** 点击（不要点在底部面板上）：

| 系统 | 操作 |
|------|------|
| **Mac** | **⇧ Shift + ⌘ Command + 左键** |
| **Windows** | **⇧ Shift + Ctrl + 左键** |

仅 **⇧ + 左键** = 复制场景 `x,z` 校准，**不会**改 LBS。

### 面板内（行中）

**回城堡**、**其它演示位置**（折叠）：`inParkGuide.js`。

---

## 四、页面与园内流程

### 布局（竖屏）

| 区域 | 说明 |
|------|------|
| **上方约 2/3** | 3D 地图（入场约 3 秒环绕镜头） |
| **底部约 1/3** | 园内助手（`styles-planner.css` → `--planner-dock-height`，默认 **33.33dvh**） |
| **面板内滚动** | 偏好、必玩、行中演示区均在面板内上下滑 |

顶栏 **横条** / **「—」** 可收起；**「偏好」** 可重开采集页。

### 推荐流程

1. 打开 http://localhost:3000  
2. **游玩偏好**：**怎么玩**（可多选，选中右下角 **✓**）— 尽量多玩 / 热门优先 / **闭园冲刺** / **亲子玩法**（展开身高）/ **排除刺激**  
3. **智能规划** → 行中引导、地图「下一站」、LBS→下一站路线、镜头聚焦  
4. **⇧⌘ 点地** 改 LBS → 推荐与路线刷新  
5. 面板内下滚 → **演示：排队 / 关闭场景**

顶栏 **行前** 仍为「稍后开放」占位。

### 本机记忆

| localStorage 键 | 作用 |
|-----------------|------|
| `disney_planner_session_id` | 恢复 session |
| `disney_planner_ready_session` | 与上者一致时才跳过偏好采集 |

---

## 五、3D 模型（设计交付）

| 事项 | 位置 |
|------|------|
| 放模型 | `frontend/assets/models/optimized/` |
| 自动加载 | `frontend/assets/data/models.manifest.json` |
| 位移旋转 | `frontend/assets/data/model_transforms.json` |
| 与地图对齐 | `geo_reference.json`、`glb_poi_bindings.json` |
| 界面开关 | `frontend/src/config.js` |

---

## 六、当前已实现（给设计验收用）

### 3D 与路线

- 3D 园区浏览、入场环绕、点击景点、排队标签（Mock）  
- 旧版 **路线规划** 面板：多点选点、高德/百度步行连线、**一键规划**（时间轴 + 可选 LLM）  

### 园内助手（Step 1 + 部分 Step 2）

- 底部 **游玩偏好** → **智能规划** → 行中下一站 / 推荐理由  
- 地图 POI：下一站高亮，其它默认收起  
- 模拟 LBS、⇧⌘ 点地、LBS→下一站步行线（失败则直线）  
- Mock 排队场景、**机会项**、亲子身高过滤、**排除刺激**过滤  
- 玩法：**闭园冲刺** 仅影响排序加分（非完整闭园策略引擎）  
- 手动 **已玩好，下一个**、刷新排队并重算  

### 数据与后端骨架

- `experience_duration_minutes`、`thrill_level`、`family_friendly` 等字段已在元数据与 DB  
- `single_rider_supported`、`priority_access_supported`、`early_entry_supported` **仅有字段，未参与推荐**  

---

## 七、常见问题

**Q：排队数字一直在变？**  
正常，每 5 分钟 mock 刷新；演示场景见 `planner_mock_scenarios.json`。

**Q：看不到偏好采集？**  
点 **偏好**，或清除 `disney_planner_session_id` 与 `disney_planner_ready_session` 后硬刷新。

**Q：面板太大/太小？**  
改 `styles-planner.css` 的 `--planner-dock-height`（默认 33.33dvh）。

**Q：尊享卡 / 单人通道选了有用吗？**  
**目前还没有采集入口和算法**，见第十节 Step 3。

---

## 八、改什么找哪个文件（速查）

| 想改… | 文件 |
|--------|------|
| Mock 排队剧本 | `backend/config/planner_mock_scenarios.json` |
| Step2 机会项 / 到站半径 | `backend/config/planner_step2.json` |
| 项目游玩时长、身高、是否支持单人/尊享/早享（标签） | `backend/data/attraction_metadata.json` |
| 怎么玩玩法与 UI | `frontend/src/ui/onboarding.js`、`goalModes.js` |
| 底部面板高度 | `frontend/src/styles-planner.css` → `--planner-dock-height` |
| LBS | `geoLocation.js`、`lbsMapPick.js` |
| 行中 UI | `inParkGuide.js`、`plannerApp.js` |
| 高德/百度/LLM Key | `backend/.env` |

---

## 九、其它文档

- 工程说明：`README.md`  
- 资源压缩：`tools/README.md`  

---

## 十、产品分期路线图（Step 1 / 2 / 3）— 写全

本节对照产品 PRD 与仓库实现，列出 **已完成**、**部分完成**、**未做（后续）**。设计排期、写交互稿时以 **「未做」** 为准，避免与当前 Demo 混淆。

### 10.1 分期总览

| 阶段 | 定位 | 状态（2026-05） |
|------|------|-----------------|
| **Step 1** | MVP：行前顺序 + 行中手动下一站 + 成本模型骨架 | ✅ 主流程可用 |
| **Step 2** | 定位、机会项、身高/关闭过滤、状态推断、地图行中体验 | ⚠️ 核心已有，多项增强未做 |
| **Step 3** | 权益修正排队、自动确认完成、规则配置化、对话采集、官方数据 | ❌ 未开发（仅预留字段/注释） |

---

### 10.2 Step 1 — 已实现

| 功能 | 说明 |
|------|------|
| 行前 / 园内 session | 匿名 `planner_sessions`、`session_preferences`、`session_progress` |
| 需求采集（简化版） | 单屏：怎么玩 + 必玩 + 入园/离园占位时间 |
| 成本模型 | `总成本 ≈ 排队 + 体验时长 + 步行`（步行为场景估算） |
| 行前规划 | 必玩优先 + 时间预算内贪心多玩 |
| 行中下一站 | `inParkDecisionService` + 手动标记已玩 |
| 重规划 | `POST /api/planner/replan` |
| Mock 排队刷新 | 定时 + 场景切换 |
| 基础 API | `session/create`、`preferences`、`pretrip-plan`、`next-recommendation` 等 |

#### Step 1 — 相对 PRD 仍简化 / 未做的部分

| 功能 | 说明 | 计划阶段 |
|------|------|----------|
| 混合式 **对话引导** 采集 | PRD 要求对话 + 卡片；当前仅卡片 | Step 3 / 增强 |
| 分步采集（日期→同行→偏好→权益→必玩） | 已合并为单屏 | 产品简化，非缺失 |
| 自动推荐「不懂项目」的候选池 | 未单独做推荐池 UI | Step 2+ |
| 可接受排队时长（30/60 分钟等） | 后端有 `max_wait_tolerance` 默认值，**无采集 UI** | Step 2+ |
| 刺激偏好三档（温和/混合/刺激） | 部分由「排除刺激」玩法代替 | Step 2+ |
| `effective_wait` 含权益 | 仍用 mock 排队原值 | **Step 3** |

---

### 10.3 Step 2 — 已实现 / 部分实现

| 功能 | 状态 | 说明 |
|------|------|------|
| **模拟 LBS** + ⇧⌘ 点地 | ✅ | 默认城堡；行中可跳演示位置 |
| **浏览器 GPS 上报** | ⚠️ | 有 `POST /api/session/location` 与坐标投影；**非默认主路径**，设计师日常用模拟 |
| 按当前位置算下一站 | ✅ | 有 LBS 时用场景坐标算步行 |
| **机会项**（排队骤降） | ✅ | `opportunityDetector.js` + 行中卡片「改去这里」 |
| 排队历史快照 | ✅ | `wait_snapshots` / `waitHistoryService` |
| **亲子身高过滤** | ✅ | 「亲子玩法」+ 身高芯片；必玩 ⛔ |
| 项目关闭过滤 | ✅ | Mock 场景 + `is_open` |
| 状态推断（前往/到达） | ✅ | `stateInferenceService`：heading / arrived / idle |
| **偏航提示** | ✅ | 靠近非推荐项目时 offRoute |
| 地图 **下一站** 高亮 + 路线 | ✅ | `waitLabels.js` + `walk-to-next` |
| 推荐理由文案 | ✅ | `reasonLines` |
| **闭园冲刺** | ⚠️ | UI 玩法已选；算法仅 **加分**，无「闭园前还能玩几个」估算 |
| 行中 **完整任务流 UI** | ⚠️ | 有下一站/机会项/手动已玩；无「当前正在玩」独立大卡 |
| 步行成本用 **真实路网** | ❌ | 仍 `SCENE_UNITS_PER_WALK_MINUTE`；地图画线可走高德/百度 |
| **自动识别玩完** + 轻确认 | ❌ | 见 Step 3 |
| **自动推断正在排队** | ❌ | 见 Step 3 |
| 演出时间窗纳入规划 | ❌ | 见下文「体验增强」 |
| 餐饮 / 休息 / 拍照插入 | ❌ | 见下文 |
| 多策略模式切换 UI | ❌ | 由「怎么玩」多选部分覆盖 |

#### Step 2 配置项（已存在，可调）

文件：`backend/config/planner_step2.json`

| 配置块 | 含义 |
|--------|------|
| `location.arrivedRadiusScene` | 判定「到达」半径 |
| `location.headingRadiusScene` | 判定「前往中」半径 |
| `location.minUpdateIntervalMs` | 定位上报节流 |
| `opportunity.*` | 机会项：降幅比例、最高等待、步行上限、加分 |
| `height.warnOnMustPlayConflict` | 必玩与身高/刺激冲突是否警告 |

---

### 10.4 Step 3 — 权益与排队修正（全部待实现）

PRD 第七章：**用户拥有的卡券必须改变「有效排队时间」**，不能写死在代码里，需 **可配置规则表**。

#### 10.4.1 需支持的权益类型（产品侧名称）

| 权益 | 用户感知 | 算法目标（PRD） | 当前仓库 |
|------|----------|-----------------|----------|
| **早享卡** | 提前入园、部分项目早间低等待 | 时间窗内 `effective_wait` 接近 0～10 分钟 | ❌ 无采集、无计算 |
| **尊享卡 / 优速通 / 单项快速通行** | 指定项目少排队或免排队 | 支持项目 `effective_wait ≈ 0`（后续可加预约时段） | ❌ 仅有 `priority_access_supported` 元数据标签 |
| **单人通道 / 单人速通** | 愿分开坐，排队更快 | `effective_wait = base_wait × 系数`（如 0.4，可配置） | ❌ 仅有 `single_rider_supported` 标签 |
| **其它官方快速通行** | 各类促销/套票权益 | 规则表扩展 | ❌ |

#### 10.4.2 需求采集（待做 UI）

| 采集项 | 说明 | 规划模块（PRD） |
|--------|------|-----------------|
| 是否拥有权益 | 无 / 早享 / 尊享 / 单人 / 其它 | `benefitSelector.js`（**未建**） |
| 尊享可用在哪些项目 | 勾选或导入套票项目列表 | `POST /api/session/benefits`（**未建**） |
| 是否接受单人通道（与同行分开） | 影响是否启用单人折算 | session `accepted_single_rider`（**未建**） |
| 早享是否计划提前入园 | 影响早享时间窗 | `has_early_entry` + 时间窗（**未建**） |

#### 10.4.3 数据与规则层（待做后端）

| 能力 | PRD 要求 | 当前 |
|------|----------|------|
| **`attraction_access_rules` 表** | 每项目：是否支持早享/尊享/单人、规则文案、折算系数、时间窗 JSON | ❌ 未建表 |
| **`entitlement` / `access_benefits` 表** | 存用户本次拥有的权益及适用项目 | ❌ |
| **`benefitRuleService.js`** | `effective_wait = adjustWaitByBenefits(base, user, rules, time)` | ❌ 仅 `costModel.js` 注释预留 |
| **配置化，不写死** | 运营可改 JSON/DB，不改算法代码 | ❌ |
| **官方规则整理** | 公开规则录入；初版可 mock | ❌ mock 规则文件也未建 |
| **API** | `GET /api/attractions/access-rules`、`POST /api/session/benefits` | ❌ |

#### 10.4.4 元数据已有、等 Step 3 接线的字段

在 `attraction_metadata.json` / DB 已种子，**推荐算法未读**：

- `single_rider_supported` — 是否支持单人通道  
- `priority_access_supported` — 是否支持尊享/优速类  
- `early_entry_supported` — 是否支持早享适用  

---

### 10.5 Step 3 — 行中状态与自动确认（全部待实现）

| 功能 | PRD 行为 | 当前 |
|------|----------|------|
| 状态 `likely_queueing` | 推断正在排队 | ❌ |
| `completed_pending_confirm` | 可能玩完，待确认 | ❌ |
| **轻确认 UI** | 「是否已玩好 XXX？」已玩好 / 还没有 / 稍后再说 | ❌（`progressConfirm.js` 未建） |
| 触发条件 | 进入半径 + 停留 ≥ 排队+体验比例 + 离开 50m+ 等 | ❌ 仅简单到达/离开推断 |
| 防骚扰 | 冷却、厕所/餐饮区不误触、停留不足不弹 | ❌ |
| 未确认不标 done | 进度可回退 | ❌ 仅手动「已玩好」 |
| `progress` 扩展字段 | `arrived_at`、`queue_started_at`、`completion_source` 等 | ❌ 表结构简化版 |

配置化阈值（PRD）：`near_radius_meters`、`leave_radius_meters`、`min_stay_ratio_for_confirm`、`cooldown_minutes` — **未建配置文件**。

---

### 10.6 Step 3 — 需求采集与行前（待实现 / 增强）

| 功能 | 说明 |
|------|------|
| **LLM 对话式引导** | 一步步问，降低门槛（与卡片混合） |
| **完整行前结果页** | 1～N 顺序卡、总排队/步行/必玩覆盖（`pretripPlanner` 有骨架，入口关闭） |
| **出行日期 / 离园** 与官方闭园联动 | `GET /api/park/hours?date=` |
| **多个孩子** + 按最小身高过滤 | 亲子仅单身高 |
| **必玩自动推荐池** | 不懂项目时系统先推候选 |
| **用户画像沉淀** | 跨次访问偏好记忆 |

---

### 10.7 官方与外部数据（待实现）

| 能力 | 说明 | 影响 |
|------|------|------|
| 迪士尼官方 **实时排队** API | 替换 `mockFetchWaitTimes` | 头顶数字、推荐可信度 |
| 迪士尼官方 **开园/闭园** | 替换 21:00 占位 | 剩余时间、闭园冲刺 |
| 演出 / 花车 **时间窗** | 纳入候选池过滤 | 不会推荐撞演出时段 |
| 项目 **排队入口坐标** | `entry_gcj_lat/lng` 与中心点分离 | 步行与到站更准 |

---

### 10.8 体验增强（PRD「MVP 后续增强」— 均未做）

| 功能 | 说明 |
|------|------|
| 餐饮 / 休息 / 拍照点插入行程 | 非骑乘 POI 插入 |
| 室内/室外、夜间优势标签参与排序 | 元数据部分有标签，未全用 |
| `night_rush_enabled` 独立开关 | 与「闭园冲刺」玩法部分重叠，完整策略未做 |
| **闭园前还能再玩几个** 估算 | 闭园冲刺模式输出 |
| 推荐日志 `recommendation_log` | 调试与复盘 |
| 账号体系 / 跨设备同步 session | 当前仅本机 localStorage |

---

### 10.9 前端模块对照（PRD 规划 vs 仓库）

| 模块 | 路径 | 状态 |
|------|------|------|
| 需求采集 | `ui/onboarding.js` | ✅ |
| 行中引导 | `ui/inParkGuide.js` | ✅ |
| Session 缓存 | `ui/sessionStore.js` | ✅ |
| 行前结果 | `ui/pretripPlanner.js` | ⚠️ 有代码，入口未开放 |
| **权益选择** | `ui/benefitSelector.js` | ❌ 未建 |
| **玩完轻确认** | `ui/progressConfirm.js` | ❌ 未建 |
| 底部宿主 | `plannerApp.js` | ✅ |

---

### 10.10 后端服务对照（PRD 规划 vs 仓库）

| 服务 | 路径 | 状态 |
|------|------|------|
| 行前规划 | `pretripPlannerService.js` | ✅ |
| 行中决策 | `inParkDecisionService.js` | ✅ |
| 机会项 | `opportunityDetector.js` | ✅ |
| 状态推断 | `stateInferenceService.js` | ⚠️ 基础版 |
| 过滤（身高/关闭/刺激） | `attractionFilterService.js` | ✅ |
| 玩法打分 | `goalModes.js` | ✅ |
| **权益规则** | `benefitRuleService.js` | ❌ 未建 |
| 元数据合并 | `attractionMetadataService.js`（PRD 名） | ⚠️ 由 seed + `Attraction` 模型承担 |
| 步行至下一站 | `walkFromPointService.js` | ✅ |
| 智能路线 LLM | `smartRouteService.js` + `llmClient.js` | ✅（路线面板，非园内助手主路径） |

---

### 10.11 必测剧本（PRD — 与设计验收相关）

| 剧本 | 预期 | 当前可测？ |
|------|------|------------|
| 热门项目排队骤降 | 升为机会项，可改道 | ✅ Mock 场景「热门降温」 |
| **权益修正（单人通道）** | 支持单人项目排序明显提前 | ❌ Step 3 |
| **尊享卡 0 等待** | 支持项目有效排队接近 0 | ❌ Step 3 |
| 亲子 100cm | 过滤 102/122 等项目 | ✅ |
| 停留后离开才确认玩完 | 不误弹确认 | ❌ Step 3 |
| 用户偏航 | 从当前位置重算 | ✅ |
| 下一站临时关闭 | 立即重排并说明 | ✅ Mock「翱翔关闭」 |

---

### 10.12 给设计师的一句话

- **现在能演示的**：3D 地图、约 1/3 底部助手、怎么玩多选、智能规划、下一站、机会项、Mock 排队、模拟 LBS。  
- **不要做成「已上线」的**：尊享卡、优速通、单人通道、早享卡、自动问「玩好了吗」、对话式问卷、迪士尼真排队/真闭园 — 均在 **Step 3 或更后**，需单独排期与视觉稿。

如需把某一 Step 3 项拆成交互稿（例如权益勾选页），可指定优先级让产品/开发对齐接口字段。
